import React from 'react';

import { AppLoadingProps } from './interfaces';

const AppLoading: React.FC<AppLoadingProps> = () => (
  <div className='App'>Loading...</div>
);

export default AppLoading;
